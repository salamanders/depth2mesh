package info.benjaminhill.math

/**
 * Simple linear algebra and geometry functions designed to operate on doubles[].
 * <ul>
 * <li>points are projected via right multiplication. E.g., p' = Mp.</li>
 * <li>Roll: rotation around X. Pitch: rotation around Y. Yaw: rotation around Z.</li>
 * <li>Roll Pitch Yaw are evaluated in the order: roll, pitch, then yaw. I.e.,
 * rollPitchYawToMatrix(rpy) = rotateZ(rpy[2]) * rotateY(rpy[1]) * rotateX(rpy[0])</li>
 * </ul>
 *
 **/

typealias EzPoint3d = EzVector

fun ezPoint3dOf(x: Double, y: Double, z: Double): EzPoint3d = doubleArrayOf(x, y, z)

fun ezPoint3dOf(x: Int, y: Int, z: Int): EzPoint3d = ezPoint3dOf(x.toDouble(), y.toDouble(), z.toDouble())

val EzPoint3d.x: Double
    get() = this[0]

val EzPoint3d.y: Double
    get() = this[1]

val EzPoint3d.z: Double
    get() = this[2]

val EzPoint3d.dimension: Int
    get() = 3

fun EzPoint3d.toVertical(): Array<DoubleArray> = arrayOf(doubleArrayOf(x), doubleArrayOf(y), doubleArrayOf(z))

fun EzPoint3d.pretty(): String = "[x:$x, y:$y, z:$z]"